
package com.snog.temporalengineering.common.temporal;

import com.snog.temporalengineering.api.TemporalAdapterRegistry;

public final class TemporalAdapters
{
    private TemporalAdapters()
    {
    }

    public static void register()
    {
        // Place your adapter registrations here, each with optional priority + id:
        // Example:
        // TemporalAdapterRegistry.register(SomeForeignBE.class, new ProgressIntAdapter(), 10, "SomeMod-ProgressAdapter");

        // No built-in adapters yet—keep this as the central entry point.
    }
}
